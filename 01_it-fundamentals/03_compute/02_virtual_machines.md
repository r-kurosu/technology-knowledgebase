# 仮想マシン

## 1. 概念理解

### 学ぶ範囲

VM、イメージ、インスタンス、OS管理、起動と停止、永続ディスク、配置。

### イメージとインスタンス

[01_process_memory_and_virtualization.md](01_process_memory_and_virtualization.md)の「プログラム vs プロセス」と同じ構造で対応づけられる。

| | プロセス（前回） | VM（今回） |
|---|---|---|
| 静的な雛形 | プログラム | イメージ（AMI） |
| 実際に動いている実体 | プロセス | インスタンス |

- イメージに入っているのはOS・インストール済みソフトウェア・設定などの**ディスクの雛形**
- スペック（vCPU数・メモリ量）を決めるのはイメージとは別レイヤー（インスタンスタイプ）の役割
- 同じイメージ（プログラム）から複数のインスタンス（プロセス）を起動できる点も共通
- インスタンスは物理マシンそのものではなく、ハイパーバイザー上で動く**仮想的な実体**（前回の「物理マシン vs 仮想マシン」の整理と一致）

### OS管理と責任共有モデル（Shared Responsibility Model）

前回の構成図（ハイパーバイザー → VM → OS/カーネル）で、OS/カーネルの層は誰が用意し管理するかを考えると境界線が見える。

| 層 | 管理者 |
|---|---|
| 物理ハードウェア・ハイパーバイザー（クラウドそのものの安全性） | AWS |
| ゲストOS・ミドルウェア・アプリ・データ（クラウドの中身） | ユーザー |

- EC2はこの境界が「ハイパーバイザーの直上」にある → OSの選択も、パッチ適用・アップデートもユーザー責任
- ECS（特にFargate）が楽なのは、この境界線がAWS側にずれてOSを選ぶ概念自体がなくなるため（EC2起動タイプのECSは裏のEC2インスタンスのOSパッチは結局ユーザー責任のまま）
- **VMの自由度と運用負荷はセット**：OSを自由に選べる代わりに、パッチ適用などの運用もセットでついてくる

### 起動と停止（Stop / Terminate）

| | Stop | Terminate |
|---|---|---|
| コンピュート（インスタンス時間）課金 | 止まる | 止まる |
| EBSストレージ課金 | 継続する | デフォルトではボリュームごと削除される（"Delete on Termination"設定次第で残せる） |
| 再開 | Startで再開可能、データはEBS上に残る | 不可（インスタンスは完全に消滅） |

### 永続ディスク（インスタンスストア vs EBS）

| | インスタンスストア | EBS |
|---|---|---|
| 物理的な位置 | ホストマシンに直接ローカル接続 | ネットワーク越しの独立したストレージサービス |
| 速度 | 速い（ローカルディスク） | ネットワーク越しなので相対的にオーバーヘッドがある |
| 永続性 | Stop・Terminate・ホスト障害で消える（揮発性） | インスタンスと切り離されて存在。Terminateしても"Delete on Termination"がfalseなら残る |
| 付け替え | 不可（そのホストに固定） | 可能（デタッチして別インスタンスにアタッチし直せる） |
| 用途 | キャッシュ・一時データ・スクラッチ領域 | OS本体・DBデータなど残したいデータ |

本質は「揮発性の程度」ではなく、**EBSがインスタンスとは別のライフサイクルを持つ独立したサービス**であること（付け替えできるのもそれが理由）。

### 配置（AZ分散・Placement Group）

- 複数AZへの分散配置の目的：**可用性向上**。各AZは物理的に独立したデータセンター群（電源・ネットワークも別系統）なので、1つのAZの障害が他に波及しない
- **Placement Group**でAZ内の配置をさらに制御できる
  - **Cluster**：低レイテンシ通信のため物理的に近くに集める（HPCなど）
  - **Spread**：異なるハードウェアに分散し、同時障害のリスクを下げる
  - **Partition**：大規模分散システム（Hadoop、Cassandraなど）向けに、パーティション単位で障害ドメインを分ける

## 2. 選択肢の比較

### 物理サーバー（ベアメタル）vs VM

物理マシンとVMの一般的な違いは[01_process_memory_and_virtualization.md](01_process_memory_and_virtualization.md)で整理済み。ここではクラウド上でベアメタル（`.metal`インスタンス）をあえて選ぶ理由を整理する。

| 理由 | 具体例 |
|---|---|
| 自分の管理下でハイパーバイザーを走らせたい | AWSのNitroを挟まずVMware ESXiやKVMなどを直接動かす（クラウド上での入れ子の仮想化） |
| 物理コア／ソケット単位のライセンス要件 | 非仮想化環境の物理コア数でライセンス計算される契約を満たすため |
| 仮想化オーバーヘッドを完全に排除したい | ハイパーバイザー経由の性能劣化すら許容できない特殊なワークロード |

ベアメタルは「自由度と運用負荷はセット」を極端にした選択で、AWSのハイパーバイザーという運用の肩代わりすら手放す。

### オンデマンド vs 長期利用（Reserved/Savings Plans）vs Spot

| | On-Demand | Reserved/Savings Plans | Spot |
|---|---|---|---|
| コミットメント | なし | 1年 or 3年の利用コミット | なし |
| 単価 | 基準 | 割引（最大〜72%） | 大幅割引（最大〜90%） |
| 引き換えに失うもの | 割引なし（柔軟性は維持） | 柔軟性（コミット分は使わなくても発生） | 可用性の保証（AWSの都合で2分前予告で中断されうる） |
| 向いている用途 | 短期・予測不能なワークロード | 常時稼働する予測可能なベースライン | フォールトトレラント／チェックポイント可能な処理（バッチ、CI/CDビルド、EMR等） |

Reservedは「需要を保証する代わりに安くする」、Spotは真逆で「供給（空きキャパシティ）が保証されない代わりに安くする」という非対称な関係。Spotはステートレス・中断されても実害がない／再開できるワークロードにしか向かない（本番DBなどステートフルな用途には不向き）。

### 汎用 vs 特化インスタンス

本質は「高性能=高コスト」の単純比例ではなく、**CPU・メモリ・ストレージの配分比率をワークロードのボトルネックに合わせる**こと。

| ファミリー | 配分の特徴 | 向いている用途 |
|---|---|---|
| 汎用（M系） | CPU:メモリのバランス型 | Webサーバー、中小規模DB、用途が定まらない開発・検証環境 |
| コンピューティング最適化（C系） | CPU比率高め | バッチ処理、動画エンコード、高スループットな推論など |
| メモリ最適化（R系） | メモリ比率高め | インメモリDB、リアルタイム分析、大規模データセットのキャッシュ |
| ストレージ最適化（I/D系） | ローカルNVMeが高速・大容量 | Hadoop、Cassandraなど高IOPSが必要な分散処理（インスタンスストアの実例） |
| GPU/アクセラレータ（G/P系） | GPU/専用チップ搭載 | 深層学習の学習・推論、グラフィックレンダリング |

不要な資源にまで対価を払わないために、ボトルネックに合わせて配分比率ごと選ぶ、という考え方。

## 3. AWSでの実装例

EC2、AMI、EBS、Launch Template、Auto Scaling Group。

## 4. アーキテクチャ図

<!-- Load Balancer、複数EC2、EBSの構成を表す -->

## 5. 設計トレードオフ

<!-- 自由度、OS運用、起動時間、性能、可用性、コストを考える -->

## 6. 自分の言葉で説明

<!-- 「EC2を選ぶ理由と運用責任」を説明する -->

## 7. 理解確認

### 到達目標

VMの自由度と運用負荷を理解し、EC2が適する要件を説明できる。

### 現在の理解度

<!-- A：説明できる / B：見れば分かる / C：まだ怪しい -->
